# Apache Iceberg Training Environment Configuration
# Copy this file to .env and adjust values as needed

# MinIO Configuration
MINIO_ROOT_USER=admin
MINIO_ROOT_PASSWORD=password
MINIO_REGION=us-east-1

# Nessie Configuration
NESSIE_PORT=19120

# Dremio Configuration
DREMIO_UI_PORT=9047
DREMIO_CLIENT_PORT=31010
DREMIO_FLIGHT_PORT=32010
DREMIO_ADMIN_PASSWORD=admin123

# MinIO Ports
MINIO_API_PORT=9000
MINIO_CONSOLE_PORT=9001

# Network Configuration
NETWORK_NAME=iceberg

# Volume Names (optional - defaults are fine)
# NESSIE_VOLUME=nessie-data
# MINIO_VOLUME=minio-data
# DREMIO_VOLUME=dremio-data

